---
phase: 01-mvp
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/utils/money.ts
  - src/utils/money.test.ts
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/styles/tokens.css
  - src/pages/HomeView.vue
  - src/pages/RecordView.vue
  - src/pages/ListView.vue
  - src/pages/DetailView.vue
  - src/pages/CategoryView.vue
  - README.md
autonomous: true
requirements: [REC-01, UX-01]
user_setup:
  - "EdgeOne Pages 控制台连接 GitHub 仓库（部署验证需用户操作）"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 金额元→分转换与格式化有自动化测试覆盖
    - 本月统计逻辑有自动化测试覆盖
    - 视觉与交互细节符合"简洁时尚、女性友好"
    - README 含使用说明与 EdgeOne 部署验证步骤
  artifacts:
    - src/utils/money.ts + 测试
    - 完善后的 tokens.css
    - README.md（使用 + 部署）
  key_links:
    - 金额工具 ← 记账页/统计/详情共用
---

<objective>
收尾打磨：为核心逻辑补自动化测试，细化视觉与交互体验，并完成使用文档与部署验证说明。

Purpose: 让 Phase 1 交付物达到可上手的质量：逻辑有测试、界面有细节、家人会用。
Output: 测试覆盖、视觉打磨、README（使用 + EdgeOne 部署验证）。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp/01-CONTEXT.md
@.planning/phases/01-mvp/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 核心逻辑测试——金额转换与本月统计（REC-01 质量保障）</name>
  <files>src/utils/money.ts, src/utils/money.test.ts, src/db/ledger.ts, src/db/ledger.test.ts</files>
  <behavior>
    - Test 1: toCents("100.5") === 10050；toCents("0.01") === 1
    - Test 2: fromCents(10050) 格式化为 "100.50"
    - Test 3: 非法输入（空、非数字、负数）抛错或被拒绝
    - Test 4: getMonthSummary 只统计当月，跨月记录不计入
  </behavior>
  <action>
    抽出 src/utils/money.ts 统一金额工具：toCents（元字符串→分整数，输入校验）、fromCents（分→元格式化，两位小数）；先写测试期望（RED）再实现（GREEN）。
    ledger.ts 的统计与金额相关路径改用 money.ts 工具，保证唯一实现；补充/调整 ledger.test.ts 覆盖 Test 4 与边界（空账本、仅当月）。
    运行全部测试确认通过；不要在组件内联金额换算逻辑（记账页/统计/详情统一走工具）。
  </action>
  <verify>
    <automated>npx vitest run src/utils/money.test.ts src/db/ledger.test.ts 通过</automated>
  </verify>
  <done>
    金额转换与本月统计均有自动化测试且通过；组件不再自行处理金额换算。
  </done>
</task>

<task type="auto">
  <name>Task 2: 视觉与交互打磨（UX-01）</name>
  <files>src/styles/tokens.css, src/pages/HomeView.vue, src/pages/RecordView.vue, src/pages/ListView.vue, src/pages/DetailView.vue, src/pages/CategoryView.vue</files>
  <action>
    按 tokens.css 令牌统一全部页面的圆角（卡片 12–16px）、字号层级、间距刻度；主按钮使用鼠尾草绿主色、白字，禁用态有明确视觉。
    交互细节：记账页金额输入聚焦即弹键盘；分类选中态高亮；列表行点击反馈；保存按钮在金额为空时禁用并给出轻提示（Vant showToast）；触控目标 ≥44px。
    风格统一检查：无突兀颜色（除白底与主色外仅少量中性灰），无溢出布局（窄屏 375px 可用）；女性友好：大字号、宽松间距、无杂乱信息。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    全部页面视觉令牌一致；关键交互（聚焦键盘、选中态、空金额禁保存）达标；375px 窄屏无溢出。
  </done>
</task>

<task type="auto">
  <name>Task 3: 使用文档与部署验证（UX-01 收尾）</name>
  <files>README.md, .planning/phases/01-mvp/SKELETON.md</files>
  <action>
    README.md 补齐：产品一句话说明、本地运行（npm install / npm run dev）、测试命令（npx vitest run）、构建（npm run build）、EdgeOne Pages 部署验证步骤（GitHub 推送 → 控制台连接仓库 → 自动构建 → 访问线上地址验证"记一笔→回首页"路径；自定义域名需确认 ICP 备案）。
    更新 SKELETON.md 的 Stack Touched 勾选与部署说明，确认与最终实现一致（项目骨架契约）。
  </action>
  <verify>
    <automated>npm run build 成功；README.md 含部署验证步骤</automated>
  </verify>
  <done>
    README 完整可用；SKELETON.md 与实现一致；部署步骤可被用户照做。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 金额输入 → 转换工具 | 用户输入的非数字/超长字符串进入金额逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | 金额输入 | medium | mitigate | toCents 输入校验（空、非数字、负数、超长拒绝）|
| T-03-02 | Tampering | npm 依赖 | high | mitigate | npm audit 通过；package-lock.json 已锁定 |
</threat_model>

<verification>
- `npx vitest run` 全部通过（金额工具 + 数据层）
- `npm run build` 通过
- 文档可读性检查：家人按 README 能跑起来
</verification>

<success_criteria>
- 金额与统计逻辑有测试兜底
- 视觉与交互细节统一达标
- README 使用说明与部署验证步骤完整
</success_criteria>

<output>
Create `.planning/phases/01-mvp/01-03-SUMMARY.md` when done
</output>
