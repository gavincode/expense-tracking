---
phase: 01-mvp
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/pages/HomeView.vue
  - src/pages/ListView.vue
  - src/pages/DetailView.vue
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/router/index.ts
autonomous: true
requirements: [REC-02, UX-01]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 用户打开首页能看到本月合计、本月笔数与最近记录（D-01）
    - 用户能进入完整支出列表，按日期倒序、按日分组
    - 用户能点开单笔支出查看金额、分类、日期、备注
    - 首次使用时有友好的空状态引导
  artifacts:
    - src/pages/ListView.vue
    - src/pages/DetailView.vue
    - 首页本月统计实现
  key_links:
    - HomeView 最近记录 → ListView 全部记录 → DetailView 单笔详情
---

<objective>
完善查看体验：首页本月总览（合计/笔数）、完整支出列表与单笔详情，并补齐空状态与视觉打磨。

Purpose: 满足 REC-02（列表与详情）与 D-01（首页总览），让用户"随时回看"不只是最近几条。
Output: 可浏览全部记录并查看单笔详情的完整视图。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 首页本月总览——合计、笔数、最近记录（D-01）</name>
  <files>src/pages/HomeView.vue, src/db/ledger.ts, src/db/ledger.test.ts</files>
  <action>
    ledger.ts 实现 getMonthSummary(yearMonth)：按 date 索引过滤当月记录，返回 { totalCents, count }；金额以分累计，展示层 fromCents 格式化（元，两位小数，千分位按中文习惯）。
    HomeView.vue 顶部展示本月合计与本月笔数两个数字卡片（统计口径为自然月，即 date 的 YYYY-MM 等于当前月），下方"最近记录"（最多 5 条）与"查看全部"入口（跳 /list）；底部保留"记一笔"大按钮（D-01）。
    补充测试：跨月记录不混入本月统计；合计金额正确。
  </action>
  <verify>
    <automated>npx vitest run src/db/ledger.test.ts 通过；npm run build 成功</automated>
  </verify>
  <done>
    首页显示本月合计（元）与笔数；最近记录最多 5 条；"查看全部"可跳转。
  </done>
</task>

<task type="auto">
  <name>Task 2: 完整支出列表——日期倒序、按日分组（REC-02）</name>
  <files>src/pages/ListView.vue, src/router/index.ts, src/db/ledger.ts</files>
  <action>
    ledger.ts 增加 listAll()：按 date 倒序、同日按 createdAt 倒序返回全部记录。
    router 增加 /list 路由 → ListView.vue。
    ListView.vue：NavBar 返回；按日分组展示（日期标题 + 当日小计），每组内为记录行（分类名、备注摘要、金额右对齐）；点击行跳 /detail/:id（REC-02）。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    列表按日期倒序分组，每日显示小计；点击记录可进入详情。
  </done>
</task>

<task type="auto">
  <name>Task 3: 单笔详情与空状态（REC-02, UX-01）</name>
  <files>src/pages/DetailView.vue, src/router/index.ts, src/pages/ListView.vue, src/pages/HomeView.vue</files>
  <action>
    router 增加 /detail/:id 路由 → DetailView.vue；ledger.ts 增加 getById(id)。
    DetailView.vue：展示金额（大字）、分类路径（如 硬装 / 水电）、日期、备注（无备注显示"无备注"）；NavBar 返回列表。
    空状态：HomeView 与 ListView 在无记录时显示 Vant Empty（插画+文案"记下第一笔装修支出"）与"记一笔"按钮（UX-01）。
    视觉打磨：按 tokens.css 令牌统一卡片圆角、间距、字号层级；触控目标 ≥44px。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    详情页四要素齐全；首次使用空状态引导明确；视觉令牌在列表/详情/空状态一致生效。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地记录 → 详情/列表渲染 | 记录内容（备注等）回显 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 详情/列表渲染 | medium | mitigate | Vue 文本插值，禁止 v-html；备注按纯文本渲染 |
| T-02-02 | Denial of Service | 大列表渲染 | low | accept | 家庭级记录量小（数千条内）；后续需要时再分页 |
</threat_model>

<verification>
- `npm run build` 通过
- `npx vitest run` 通过（本月统计测试）
- 手动路径验证：首页总览 → 查看全部 → 列表分组 → 进入详情
</verification>

<success_criteria>
- 首页总览口径正确（自然月）
- 全部记录可按日浏览，单笔详情完整
- 空状态与视觉令牌达标
</success_criteria>

<output>
Create `.planning/phases/01-mvp/01-02-SUMMARY.md` when done
</output>
