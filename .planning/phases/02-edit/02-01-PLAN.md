---
phase: 02-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/router/index.ts
  - src/pages/RecordView.vue
  - src/pages/DetailView.vue
  - src/stores/draft.ts
autonomous: true
requirements: [REC-03]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 用户能从详情页进入编辑，表单预填原记录
    - 用户修改金额/分类/日期/备注后保存，详情页与列表同步更新
    - 编辑不会产生重复记录（更新而非新增）
  artifacts:
    - updateExpense（ledger.ts）
    - RecordView 编辑模式（/edit/:id）
    - DetailView 编辑入口
  key_links:
    - DetailView 编辑按钮 → /edit/:id → RecordView 预填 → updateExpense → 回详情
---

## Phase Goal

**As a** 装修中的家庭成员，**I want to** 随时修正已记错的支出（金额/分类/日期/备注），**so that** 账本始终准确可信。

<objective>
打通"详情 → 编辑 → 保存 → 同步更新"的端到端路径，让用户能修正任意一笔支出。

Purpose: 满足 REC-03 的编辑能力，复用 Phase 1 记账表单，避免重复开发。
Output: 支出编辑功能（表单复用 + 数据更新）。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-edit/RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 端到端编辑路径——详情→编辑→保存→详情更新</name>
  <files>src/db/ledger.ts, src/db/ledger.test.ts, src/router/index.ts, src/pages/RecordView.vue, src/pages/DetailView.vue</files>
  <action>
    ledger.ts 增加 updateExpense(id, fields)：按 id 更新金额/分类/日期/备注并刷新 updatedAt；保留 createdAt。
    router 增加 /edit/:id 路由 → RecordView（name: edit）。
    RecordView 支持编辑模式：route.params.id 存在时 onMounted 加载记录（getById），将金额（fromCents 还原为输入串）、分类（用记录的 categoryId/categoryPath 回填 categoryStore 与活动组/子项）、日期、备注预填进 draft；保存时若为编辑模式调用 updateExpense 并 router.replace(`/detail/${id}`)，否则保持新增逻辑。
    DetailView 增加"编辑"按钮 → router.push(`/edit/${id}`)。
    测试：updateExpense 后 getById 返回更新值且 createdAt 不变、updatedAt 变化；新增测试用例。
  </action>
  <verify>
    <automated>npx vitest run src/db/ledger.test.ts 通过；npm run build 成功</automated>
  </verify>
  <done>
    从详情页可进入编辑、表单预填正确、保存后详情显示新值且无重复记录。
  </done>
</task>

<task type="auto">
  <name>Task 2: 编辑表单的分类回填与保存反馈</name>
  <files>src/pages/RecordView.vue, src/pages/DetailView.vue</files>
  <action>
    编辑模式回填分类：若记录的 categoryPath 能匹配到预设或自定义分类（按 categoryId），选中对应一级并高亮对应二级；否则按"未分类"处理并在保存时保留原 categoryPath 文本。
    保存按钮文案在编辑模式显示"保存修改"；保存成功后 showToast('已更新') 并回详情页。
    金额输入串还原：fromCents(amountCents) 去掉末尾多余的 0 或保留两位小数均可，确保可编辑。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    编辑时分类/金额/日期/备注全部正确回填；保存后提示"已更新"并回详情。
  </done>
</task>

<task type="auto">
  <name>Task 3: 编辑流程收尾与回归</name>
  <files>src/db/ledger.ts, src/db/ledger.test.ts, src/pages/ListView.vue</files>
  <action>
    回归检查：编辑保存后返回详情，再从详情返回列表，列表行数据为更新后的值（列表从 listAll 读取，天然一致；确认无缓存问题）。
    补充测试：编辑后 listAll 返回更新后的记录（金额/备注更新）。
    无数据残留：编辑不新增行。
  </action>
  <verify>
    <automated>npx vitest run 通过；npm run build 成功</automated>
  </verify>
  <done>
    编辑链路全通且列表/详情/统计一致；新增测试覆盖更新场景。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编辑输入 → 本地记录更新 | 用户修改内容写入 IndexedDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 金额编辑输入 | medium | mitigate | 复用 toCents 校验，非法金额拒绝保存 |
| T-02-02 | Denial of Service | 并发编辑 | low | accept | 家庭单机场景，最后写入生效 |
</threat_model>

<verification>
- `npx vitest run` 通过（更新语义测试）
- `npm run build` 通过
- 手动路径：详情 → 编辑 → 改金额/分类 → 保存 → 详情/列表显示新值
</verification>

<success_criteria>
- 编辑功能全链路可用，不产生重复记录
- 表单回填正确，保存反馈明确
</success_criteria>

<output>
Create `.planning/phases/02-edit/02-01-SUMMARY.md` when done
</output>
