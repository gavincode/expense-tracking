---
phase: 02-edit
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/router/index.ts
  - src/pages/RecordView.vue
  - src/pages/CategoryManageView.vue
autonomous: true
requirements: [REC-05]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 用户能重命名自定义分类（一级/二级），记账时使用新名称
    - 用户能删除自定义分类，删除后选择列表不再显示
    - 删除/重命名不影响历史支出的分类快照
  artifacts:
    - 分类管理页（/categories-manage）
    - 分类重命名/软删除数据操作
    - RecordView 分类模块管理入口
  key_links:
    - RecordView 管理入口 → /categories-manage → 重命名/删除 → 返回后选择列表刷新
---

<objective>
补齐自定义分类的管理能力：重命名与删除（添加已在 Phase 1 完成）。

Purpose: 完成"自由扩展分类"的闭环，避免无法清理的自定义分类堆积。
Output: 分类管理页 + 重命名/软删除。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-edit/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 分类管理数据层——重命名与软删除</name>
  <files>src/db/ledger.ts, src/db/ledger.test.ts</files>
  <action>
    ledger.ts 增加 renameCategory(id, name)：更新自定义分类名称；preset 分类不可重命名（忽略）。
    增加 deleteCategory(id)：软删除（deleted=1），查询 listCustomCategories 时过滤已删除；历史支出保留原 categoryPath 快照。
    测试：重命名后 listCustomCategories 返回新名称；删除后不再返回且记录仍在表内。
  </action>
  <verify>
    <automated>npx vitest run src/db/ledger.test.ts 通过；npm run build 成功</automated>
  </verify>
  <done>
    重命名/软删除数据操作可用且有测试。
  </done>
</task>

<task type="auto">
  <name>Task 2: 分类管理页面</name>
  <files>src/pages/CategoryManageView.vue, src/router/index.ts</files>
  <action>
    新建 CategoryManageView：NavBar"分类管理"；列出所有自定义分类（一级组 + 其下二级，按组展示）；每行提供"重命名"与"删除"操作（Vant 弹窗输入新名称 / showConfirmDialog 确认删除）。
    重命名后列表即时刷新；删除后该项消失。
    router 增加 /categories-manage 路由。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    管理页可用：可重命名/删除自定义分类并即时刷新。
  </done>
</task>

<task type="auto">
  <name>Task 3: 记账页管理入口与联动刷新</name>
  <files>src/pages/RecordView.vue</files>
  <action>
    RecordView 分类模块标题右侧增加"管理"文字链接 → /categories-manage。
    返回记账页时重新加载自定义分类（loadCustom 在 onActivated/返回时执行或管理页返回后刷新），确保重命名/删除即时反映到选择列表。
    删除当前选中的分类时，选择回退到"未分类"。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    记账页可进入管理；管理后选择列表正确刷新；被删分类回退未分类。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理操作 → 本地分类数据 | 重命名/删除影响后续记账选择 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | 分类删除 | low | mitigate | 二次确认；软删除；历史支出快照不受影响 |
| T-02-06 | Tampering | 分类重命名 | low | mitigate | 名称非空校验 + 重名拦截（同层级） |
</threat_model>

<verification>
- `npx vitest run` 通过（分类重命名/删除测试）
- `npm run build` 通过
- 手动路径：记一笔 → 管理 → 重命名/删除 → 返回选择列表刷新
</verification>

<success_criteria>
- 自定义分类可管理（重命名/删除），历史数据不受影响
- 记账页联动刷新正确
</success_criteria>

<output>
Create `.planning/phases/02-edit/02-03-SUMMARY.md` when done
</output>
