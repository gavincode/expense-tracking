---
phase: 02-edit
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/pages/DetailView.vue
autonomous: true
requirements: [REC-03]
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 用户能从详情页删除一笔支出，删除前有确认提示
    - 删除后支出列表、最近记录、本月统计不再包含该笔
    - 删除采用软删除（tombstone），为 Phase 3 同步预留
  artifacts:
    - deleteExpense（ledger.ts）
    - expenses 表 deleted 索引（schema v3）
    - DetailView 删除按钮 + 确认弹窗
  key_links:
    - DetailView 删除 → showConfirmDialog → deleteExpense → 回列表
---

<objective>
让用户能安全地删除一笔支出，删除后所有视图一致消失。

Purpose: 满足 REC-03 的删除能力；软删除避免 Phase 3 同步时"删了又回来"。
Output: 删除功能（确认 + 软删除 + 全视图过滤）。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-edit/RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 软删除数据层——deleted 标记与查询过滤</name>
  <files>src/db/ledger.ts, src/db/ledger.test.ts</files>
  <action>
    Dexie schema 升级到 version 3：expenses 增加 `deleted` 索引（0/1，默认 0）；categories 增加 `deleted` 字段（默认 0，供 Plan 03 使用）。
    ledger.ts 增加 deleteExpense(id)：将记录 marked deleted=1 并刷新 updatedAt（不物理删除）。
    所有查询（listAll/listRecent/getMonthSummary/getById）过滤 deleted=1 的记录；getById 返回非删除记录，删除后返回 undefined。
    测试：删除后 listAll/listRecent/getMonthSummary/getById 均不含该记录；记录仍在表中（软删除校验）。
  </action>
  <verify>
    <automated>npx vitest run src/db/ledger.test.ts 通过；npm run build 成功</automated>
  </verify>
  <done>
    软删除生效：所有视图过滤已删除记录，表中保留 tombstone。
  </done>
</task>

<task type="auto">
  <name>Task 2: 详情页删除入口与确认弹窗</name>
  <files>src/pages/DetailView.vue</files>
  <action>
    DetailView 底部增加"删除"按钮（红色/次要样式），点击弹出 Vant showConfirmDialog 确认（标题"删除这笔支出？"，文案"删除后不可恢复"）。
    确认后调用 deleteExpense(id)，showToast('已删除')，router.replace('/list')（无历史时回首页）。
    记录已删除或不存在时显示空状态（现有逻辑）。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    删除需二次确认；确认后回列表，该笔不再出现。
  </done>
</task>

<task type="auto">
  <name>Task 3: 删除回归与测试收尾</name>
  <files>src/db/ledger.ts, src/db/ledger.test.ts</files>
  <action>
    补充测试：删除后本月统计不计入该笔；多笔删除互不影响。
    回归：编辑已被删除的记录 id 时 getById 返回 undefined，编辑页应回列表并提示（RecordView 编辑模式加载失败处理）。
  </action>
  <verify>
    <automated>npx vitest run 通过；npm run build 成功</automated>
  </verify>
  <done>
    删除语义完整（列表/统计/详情一致），编辑已删除记录有兜底。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除操作 → 本地记录 | 误删不可恢复（界面层面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Repudiation | 删除操作 | low | mitigate | 二次确认弹窗防误删；软删除保留数据 |
| T-02-04 | Tampering | tombstone 同步 | low | accept | 单机场景；Phase 3 同步时按 updatedAt 合并 |
</threat_model>

<verification>
- `npx vitest run` 通过（软删除语义测试）
- `npm run build` 通过
- 手动路径：详情 → 删除 → 确认 → 回列表且记录消失；统计同步减少
</verification>

<success_criteria>
- 删除有确认、有反馈、全视图一致
- 软删除 tombstone 落地，为同步预留
</success_criteria>

<output>
Create `.planning/phases/02-edit/02-02-SUMMARY.md` when done
</output>
