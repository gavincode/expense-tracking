---
phase: 03-share
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/db/sync.ts
  - src/db/ledger.ts
  - src/db/ledger.test.ts
  - src/composables/useSync.ts
  - src/pages/HomeView.vue
  - src/pages/ListView.vue
  - src/pages/RecordView.vue
autonomous: true
requirements: [SHAR-03]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 家人设备在 30–60 秒内看到对方的新增/编辑/删除（SHAR-03）
    - 弱网/离线可记账，联网后自动补同步（D-08）
    - 页面激活时立即拉取一次
  artifacts:
    - src/composables/useSync.ts（轮询调度）
    - sync 增量拉取与离线队列
  key_links:
    - useSync 定时 pull → 合并 Dexie → 界面自动刷新
    - 本地队列 → 联网 push → 服务端合并
---

<objective>
实现定时轮询同步与离线补同步，让多设备数据在 30–60 秒内一致。

Purpose: 满足 SHAR-03 与阶段成功标准 2/4（D-02, D-08）。
Output: 自动同步（轮询 + 离线队列）。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share/03-CONTEXT.md
@.planning/phases/03-share/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定时轮询与增量拉取（D-02, SHAR-03）</name>
  <files>src/composables/useSync.ts, src/db/sync.ts, src/pages/HomeView.vue, src/pages/ListView.vue</files>
  <action>
    新建 useSync composable：进入账本后启动轮询（默认 30–60 秒，取 config/常量），页面激活（visibilitychange 到 visible 或路由进入）时立即拉取一次；组件卸载时清理定时器。
    sync.pullLedger 支持增量：记录本地 lastSyncedAt，请求携带 since，服务端（或客户端合并时）只取 updatedAt > since 的记录；合并后更新 lastSyncedAt。
    HomeView/ListView 在拉取完成后自动刷新数据（复用现有加载函数）。
    拉取失败静默重试（下次轮询），不打扰用户。
  </action>
  <verify>
    <automated>npx vitest run 通过；npm run build 成功</automated>
  </verify>
  <done>
    轮询与页面激活拉取生效；增量合并正确；界面自动更新。
  </done>
</task>

<task type="auto">
  <name>Task 2: 离线队列与自动补同步（D-08）</name>
  <files>src/db/sync.ts, src/db/ledger.ts, src/db/ledger.test.ts, src/pages/RecordView.vue</files>
  <action>
    本地写操作（新增/编辑/删除）进入待同步队列（Dexie 表 sync_queue，记录类型与目标 id）；保存时若在线立即 push，离线则保留队列。
    轮询/激活时若队列非空先 push（push 含本地更新，含 tombstone），成功后清队列；409 冲突按 Plan 01 策略重试。
    RecordView 保存后提示"已保存（待同步）"当离线时；在线保存静默同步。
    补充测试：离线入队、联网后推送清队、删除 tombstone 入队。
  </action>
  <verify>
    <automated>npx vitest run src/db/ledger.test.ts 通过；npm run build 成功</automated>
  </verify>
  <done>
    离线记账可入队，联网后自动补同步；删除正确同步。
  </done>
</task>

<task type="auto">
  <name>Task 3: 同步状态与回归</name>
  <files>src/composables/useSync.ts, src/pages/HomeView.vue</files>
  <action>
    首页展示轻量同步状态（"同步中/已同步/离线"）或仅在失败时提示，不打断记账。
    回归：多设备场景模拟测试（两个本地库实例合并）纳入单测；确认编辑/删除跨设备一致。
  </action>
  <verify>
    <automated>npx vitest run 通过；npm run build 成功</automated>
  </verify>
  <done>
    同步状态可见且不打扰；多设备一致性有测试覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端轮询 → API | 频繁读取账本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Denial of Service | 轮询频率 | low | mitigate | 30–60s 间隔 + 失败退避 |
| T-03-07 | Tampering | 离线队列重复推送 | medium | mitigate | push 幂等（按记录 updatedAt 合并），成功后清队 |
</threat_model>

<verification>
- `npx vitest run` 通过（队列/合并测试）
- `npm run build` 通过
- 手动路径：两台设备加入同一账本，一端记账/编辑/删除，另一端 30–60s 内看到变化；断网记账恢复后自动同步
</verification>

<success_criteria>
- 轮询同步与离线补同步完整可用
- 多设备一致性达标
</success_criteria>

<output>
Create `.planning/phases/03-share/03-03-SUMMARY.md` when done
</output>
