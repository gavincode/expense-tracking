---
phase: 03-share
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - functions/api/invite.ts
  - src/api/client.ts
  - src/pages/JoinView.vue
  - src/pages/HomeView.vue
  - src/router/index.ts
  - src/db/ledger.ts
  - src/db/ledger.test.ts
autonomous: true
requirements: [SHAR-01]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 创建者可查看邀请链接、二维码与短码
    - 家人打开链接/扫码/输入短码后直接加入账本（无需登录）
    - 加入后设置自己的昵称并进入账本
  artifacts:
    - functions/api/invite.ts（校验邀请码并加入）
    - src/pages/JoinView.vue（加入页）
    - HomeView 邀请展示（链接/二维码/短码）
  key_links:
    - HomeView 邀请 → JoinView 扫码/短码 → POST /api/invite → 写入成员 → 进入账本
---

<objective>
实现邀请加入：创建者展示链接/二维码/短码，家人打开后直接加入并设置昵称。

Purpose: 满足 SHAR-01 与阶段成功标准 1（邀请加入，无需登录，D-04/D-05）。
Output: 邀请展示 + 加入流程。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-share/03-CONTEXT.md
@.planning/phases/03-share/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 邀请 API 与展示（D-04）</name>
  <files>functions/api/invite.ts, src/api/client.ts, src/pages/HomeView.vue</files>
  <action>
    functions/api/invite.ts 增加 POST /api/invite：接收 inviteCode + 请求方 deviceId，校验短码存在且匹配账本，将 deviceId 写入账本 members（若不存在），返回 ledgerId。
    HomeView 账本信息区增加"邀请家人"入口：弹出面板展示邀请链接（https://<domain>/join?code=XXXX）、6 位短码、二维码（使用 qrcode 库将链接渲染为图片，前端生成）。
    邀请码长期有效（D-04）；链接/二维码/短码三者指向同一加入流程。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    邀请面板可用：链接/二维码/短码齐全；POST /api/invite 正确写入成员。
  </done>
</task>

<task type="auto">
  <name>Task 2: 加入页与昵称设置（D-05, D-06）</name>
  <files>src/pages/JoinView.vue, src/router/index.ts, src/api/client.ts</files>
  <action>
    新建 JoinView（路由 /join，支持 ?code= 参数）：展示"加入装修账本"；打开链接自动携带 code 调用加入 API；也提供短码输入框（扫码进入等价于带 code 的链接）。
    加入成功后进入昵称设置页/步骤（默认"我"，可修改），确认后保存 deviceId+nickname 到本地并写入成员，进入首页。
    非账本成员访问首页时引导创建或加入（首页判断本地 ledgerId/deviceId 是否已加入）。
  </action>
  <verify>
    <automated>npm run build 成功；npx vitest run 通过</automated>
  </verify>
  <done>
    打开链接/扫码/输短码均能加入；加入后设置昵称并进入账本首页。
  </done>
</task>

<task type="auto">
  <name>Task 3: 加入流程回归与测试</name>
  <files>src/db/ledger.test.ts, src/api/client.ts</files>
  <action>
    补充测试：invite 校验（错误短码拒绝、重复加入幂等）、成员写入。
    回归：加入后首次拉取账本数据（复用 sync.pullLedger），首页显示已有记录。
  </action>
  <verify>
    <automated>npx vitest run 通过；npm run build 成功</automated>
  </verify>
  <done>
    加入流程全链路可用且有测试；首次拉取正确。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 邀请码 → 加入 API | 未验证身份的设备加入账本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | 邀请码滥用 | low | accept | 家庭场景；短码随机 6 位；后续 v2 可加有效期/一次性 |
| T-03-05 | Repudiation | 成员写入 | low | mitigate | deviceId 幂等，重复加入不产生重复成员 |
</threat_model>

<verification>
- `npx vitest run` 通过（invite/成员测试）
- `npm run build` 通过
- 手动路径：创建者展示邀请 → 家人打开链接/扫码/输短码 → 设昵称 → 进入账本
</verification>

<success_criteria>
- 邀请（链接/二维码/短码）与加入流程完整可用
- 无需登录，加入后设置昵称
</success_criteria>

<output>
Create `.planning/phases/03-share/03-02-SUMMARY.md` when done
</output>
